export enum DifficultyLevel {
  LOW = 'Fácil',
  MEDIUM = 'Medio',
  HIGH = 'Difícil',
}
export enum TimeMode {
  SHORT = 30,
  MEDIUM = 60,
  LONG = 90,
}

export enum TypeSelect {
  LEVEL = 'level',
  TIME = 'time',
}
